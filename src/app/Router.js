import Router from 'express';

const router = Router();

router.post('/posts', () => {});

router.get('/posts', () => {});

router.get('/posts/:id', () => {});

router.put('/posts', () => {});

router.delete('/posts', () => {});
